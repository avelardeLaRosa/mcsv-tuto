server:
  port: 8083
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/avelardeLaRosa/mcsv-tuto
          search-paths: config-data
